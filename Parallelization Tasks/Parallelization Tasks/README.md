## Pixel format


## 24-битный формат пикселов (24bpp)
поддерживает 16 777 216 различных цветов и сохран€ет 1 пиксельное значение в 3 байтах.

 аждый пиксел определ€ют красный, зеленый и синий образцы пиксел€:
```
8.8.8. 0. 0 в нотации RGB A X
```

¬ пор€дке: синий, зеленый и красный (8 бит на каждый образец).

